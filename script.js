const cfg=window.EED_CONFIG;const slides=[...document.querySelectorAll(".slide")],dots=document.querySelector(".dots");let current=0,timer;slides.forEach((_,i)=>{let b=document.createElement("button");b.onclick=()=>show(i);dots.appendChild(b)});function show(i){current=(i+slides.length)%slides.length;slides.forEach((s,j)=>s.classList.toggle("active",j===current));[...dots.children].forEach((d,j)=>d.classList.toggle("active",j===current));clearInterval(timer);timer=setInterval(()=>show(current+1),5000)}document.querySelector(".next").onclick=()=>show(current+1);document.querySelector(".prev").onclick=()=>show(current-1);show(0);
const grid=document.getElementById("products"),search=document.getElementById("search"),buttons=[...document.querySelectorAll("#filters button")];let filter="all";function render(){let q=search.value.toLowerCase().trim(),items=cfg.products.filter(p=>(filter==="all"||p.category===filter)&&(!q||(p.name+" "+p.description).toLowerCase().includes(q)));grid.innerHTML=items.map(p=>`<article class="card"><div class="pic">ภาพเมนูจริง<br><small>${p.name}</small></div><div class="body">${p.badge?`<span class="badge">${p.badge}</span>`:""}<h3>${p.name}</h3><p>${p.description}</p><span class="price">${p.price} บาท</span></div></article>`).join("")}buttons.forEach(b=>b.onclick=()=>{buttons.forEach(x=>x.classList.remove("active"));b.classList.add("active");filter=b.dataset.f;render()});search.oninput=render;render();
const menuBtn=document.getElementById("menuBtn"),nav=document.getElementById("nav");menuBtn.onclick=()=>nav.classList.toggle("open");document.querySelectorAll("nav a").forEach(a=>a.onclick=()=>nav.classList.remove("open"));
document.getElementById("quote").onsubmit=async e=>{e.preventDefault();let d=Object.fromEntries(new FormData(e.target).entries()),m=`ขอใบเสนอราคาจัดเบรก EED Salapao\nชื่อ: ${d.name}\nโทร: ${d.phone}\nวันที่: ${d.date||"-"}\nจำนวน: ${d.qty||"-"} ชุด\nงบต่อชุด: ${d.budget}\nรายละเอียด: ${d.details||"-"}`;try{await navigator.clipboard.writeText(m);document.getElementById("status").textContent="คัดลอกข้อความแล้ว กรุณาส่งให้ร้านผ่าน LINE หรือโทรศัพท์"}catch{document.getElementById("status").textContent=m}};